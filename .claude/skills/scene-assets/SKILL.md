---
name: scene-assets
description: Regenerate the arvandsport 3D scene's assets from their sources — the Trionda ball's panels, the owner's figure, and the hero's rotation sprite. Use this whenever a new photo, .glb, .obj or .fbx is supplied for the site, when asked to repaint or rebuild the owner or the ball, when the ball shown before the scene loads is the wrong one, or when anything in assets-src/ changes. Every asset in the scene is generated by a tool in tools/ with arguments that matter; none of them should be produced by hand.
---

# Regenerating the scene's assets

Nothing in `experience/public/models/` or the hero sprite is authored. Each one
is the output of a tool in `tools/`, run against a file in `assets-src/`. The
tools carry the reasoning in their own docstrings — read the one you are about
to run — and this is the map of which to reach for.

## What generates what

| Ships as | From | Command |
|---|---|---|
| `experience/public/models/soccer-ball.glb` | `assets-src/trionda.glb` | `python3 tools/glb-panels.py` |
| `experience/public/models/owner.glb` | `assets-src/owner-cutout.png` | `npm run owner:relief` in `experience/` |
| `static/assets/img/ui/ball-sheet.webp` and `ball-still.webp` | the ball GLB above | `node tools/render-sprite.mjs` |

After any of them: rebuild. `npm run build` at the root copies
`experience/public/` into `dist/assets/scene/`, and `dist/` is committed.

## The owner

`npm run owner:relief` (in `experience/`) builds a solid from one photograph:
it cuts him out, inflates the silhouette into a shell and lays the photograph
back over it, so the texture registers exactly because the outline *is* the
photograph's. Read `tools/relief-from-photo.py` before changing its numbers.

A new photo goes to `assets-src/owner-cutout.png`. It should be cropped close
to him; the mask comes from colour, so a cut-out with real transparency or on a
near-white ground is used directly and is much better than a crop of a busy
scene.

**Check the mask, not just the model.** Every bad result from this pipeline has
been a mask failure, and they all look like modelling failures:

```bash
python3 tools/prepare-photo.py assets-src/owner-photo-original.jpg \
  experience/public/models/owner.glb /tmp/out.jpg --preview /tmp/plate
```

`/tmp/plate/mask.png` is what the tool thinks he is. If his hair is missing, the
top of his head will be missing from the *mesh* too, and the background will
have bled over it.

The older route is still here and still works: `npm run owner` decimates the
Tripo scan at `assets-src/owner.glb` and `npm run owner:paint` projects the
photograph onto it. That version is genuinely round; the relief is a relief.
Use the scan if the figure ever has to turn far from the camera.

## The ball

`tools/glb-panels.py` recovers the Trionda's four panels by flooding across
faces whose dihedral angle is under the crease threshold. The one thing to know
before touching it: the mesh is un-welded for its UVs — 20,028 vertices for
3,340 positions — so adjacency has to be computed on welded positions, or every
texture seam reads as a crease and the ball comes apart into hundreds of
pieces. The panel count is the check: four, of roughly 1,670 triangles each.

## The sprite

`tools/render-sprite.mjs` renders the 30-frame rotation sheet **from the same
GLB the WebGL scene loads**, in a real browser with three.js. That is the point
of it: when the sprite was a render of a different ball, the page opened on one
ball and swapped it for another a second later, and phones — which never get
the scene — saw the wrong ball permanently.

```bash
PLAYWRIGHT_PATH=/path/to/node_modules/playwright/index.mjs \
  node tools/render-sprite.mjs
```

Re-render it whenever the ball model changes. There is no check that will catch
a stale sprite for you.

## After any of this

Run the `story-check` skill. These assets are drawn on a canvas, so the only
way to know they are right is to look at the page.
